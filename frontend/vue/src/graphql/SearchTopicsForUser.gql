query searchTopicsForUser ($topicsName: String! $username: String!) {
  kgraph_topics(where: {users_username: {_eq: $username} , name: {_like: $topicsName}}) {
    id
    name
    content
    parent_id
    tags
  }
}
